---
const currentPath = Astro.url.pathname;

const navItems = [
  { href: "/", label: "Inicio" },
  { href: "/imoveis/", label: "Imoveis" },
  { href: "/contato/", label: "Contato" },
];
---

<header class="site-header">
  <div class="container topbar">
    <a class="brand" href="/">
      <span class="brand-mark">IT</span>
      <span>
        <strong>Imoveis Teresina</strong>
        <small>Meus anuncios em Teresina-PI</small>
      </span>
    </a>

    <nav aria-label="Principal" class="main-nav">
      {navItems.map((item) => (
        <a class={currentPath === item.href ? "active" : ""} href={item.href}>
          {item.label}
        </a>
      ))}
    </nav>

    <a
      class="button button-primary header-cta"
      href="https://wa.me/5586999254624?text=Ola%2C%20quero%20mais%20informacoes%20sobre%20os%20imoveis."
      target="_blank"
      rel="noreferrer"
    >
      Falar no WhatsApp
    </a>
  </div>
</header>
